module.exports = {
    presets: [
        'babel-preset-expo',  // Para projetos Expo (React Native)
        '@babel/preset-react', // Para transformar JSX (React)
    ],
    plugins: [
        'react-native-reanimated/plugin',
    ],
};
